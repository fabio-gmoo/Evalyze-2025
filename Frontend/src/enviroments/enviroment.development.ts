export const environment = {
  production: false,
  // si usarás proxy (recomendado), deja relativo:
  apiBase: 'http://localhost:9000',
  // si NO usarás proxy, apunta directo:
  // apiBase: 'http://localhost:9000',
};
